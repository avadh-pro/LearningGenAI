# Recall — 2026-09-09 (Tuesday)

*Purpose of this file: a fresh-recall brief for the very next session, so you can pick up exactly where today left off without re-reading the whole conversation. One file per day, in this `Daily Recall/` folder, named by date. This replaces the earlier `PROGRESS_LOG.md` approach.*

**Branch for almost everything below:** `LearningGenAI-Week3`. Two other branches were also created today (details in their own section below). Everything described here is committed and pushed — last commit on `LearningGenAI-Week3` today: `16a0a9e`.

---

## 1. RAG Workflow document
**File:** `Retrieval Augmented Generation (RAG)/RAG Workflow - Three Steps.md`

- Step 1 (retrieval) reached **Q1–Q12** in its Q&A section: dense vector retrieval, other semantic search methods, keyword vs. meaning search, where each hybrid-search algorithm actually runs, how a matched embedding maps back to text, the bi-encoder → cross-encoder pipeline and their independence, two mind-map passes (the first corrected after Q9–Q10 exposed an ambiguity), and how quantization differs from ANN.
- A full **"Step 1 Interview Prep" mock interview** was added — 10 interviewer questions with model answers and follow-ups, calibrated to ~4 years' AI engineering experience, later simplified in place (the HNSW/ANN answer broken down with a numeric example, the evaluation-metrics answer rebuilt around a fishing analogy after follow-up questions on MRR vs. NDCG).
- The **Step 2 deep dive** (context augmentation, transformer architecture, attention, context window) was filled in, plus Q1–Q3 there (model weights vs. attention scores, the KV cache, and how retrieval embeddings relate to attention).
- **Step 3 (Generate a Response) is still completely empty** — no deep dive, no interview prep, no Q&A. This is the most obvious next thing to build.

## 2. Consolidated Q&A across all three weeks (separate branch)
- Branch **`LearningGenAI-ConsolidatedQA`** (from `main`, not from any week branch) holds `Consolidated-QA.md` — all 122 Q&A pairs from Week 1, 2, and 3, extracted verbatim, grouped by week → source file.
- Also uploaded as a plain `.md` file to Google Drive.
- One-time deliverable; nothing pending here unless new weeks get added later.

## 3. Vector Database document
**File:** `Introduction to Vector Database/Introduction to Vector Database.md`

- Added a web-researched **11-question mock interview**, then rewrote it in plain language with an everyday example per question (Google Photos, shopping carts, food delivery, hiring pipelines, retail warehouses, GPS debugging).
- Added a **"🎯 Standard Interview Answer"** to every question — additive, sits alongside the plain-language version.
- Layered in several deeper, additive clarifications afterward: a clear cosine/dot-product/Euclidean decision rule with real examples, a simpler HNSW/IVF analogy plus a term-by-term IVF breakdown, a full vector-DB use/don't-use table with a jargon glossary (including correcting that Reciprocal Rank Fusion is not a search algorithm like ANN), and a mechanism-level breakdown of what metadata is and how pre-filtering technically works.
- New **"💬 Spontaneous Questions"** section at the end — questions asked mid-review get documented separately here by default, and only fold into the prepared interview set if explicitly asked. Currently has SQ1 (SQL vs. NoSQL vs. its own category).
- **`ChromaDB_tutorial.ipynb`** annotated cell-by-cell (30 code cells).

## 4. LangChain and LangGraph
- New file **`LangChain/LangChain Fundamentals - Video Notes.md`** — notes from an intro video (the problem plain LLM calls can't solve, a support-chatbot example, the five core components: Chains, Models, Prompts, Memory, Agents).
- **`LangChain/LangChain.md`** — added **Q2** (LCEL, declarative vs. imperative, confirming `chain = prompt | model | parser` is real code), and — the last thing done today — a full **web-researched "🎤 Interview Prep" mock interview covering both LangChain and LangGraph** (13 questions: chain vs. agent, LCEL/Runnable migration, memory-type trade-offs, tool-calling mechanics, the `AgentExecutor` → `create_agent` deprecation, structured output, a production failure mode, then LangGraph's state/nodes/edges, conditional routing, checkpointing, human-in-the-loop, and the supervisor multi-agent pattern), in the same two-layer plain-language + Standard Interview Answer format as the Vector Database section.
- Two notebooks annotated cell-by-cell: `Langchain_Setup_and_Simple_Chain- DONE.ipynb` (renamed from its original filename outside this session — content unchanged) and `Langchain_Chains.ipynb` (the Mistral-7B / 4-bit quantization / HuggingFacePipeline example).
- New file **`Langchain_Setup_and_Simple_Chain_LCEL.ipynb`** — the joke-chain example rewritten in LCEL pipe style, with a side-by-side comparison table against the original `LLMChain` version.
- **`Langchain_LCEL.ipynb`** (the pre-existing notebook in the folder) has **not** been annotated yet.

## 5. Consolidated interview-questions file (separate deliverable)
- New file **`RAG and Vector Database - Interview Questions.md`** at the repo root — combines the RAG Workflow's Step 1 mock interview and the Vector Database's mock interview into one standalone document, content verbatim. Also uploaded to Google Drive.
- Does **not** yet include today's new LangChain/LangGraph interview section (that was added afterward) — worth folding in if a fully consolidated interview file is wanted later.

## 6. A separate, unrelated branch: `OpenAI-FDE-Interview-Prep`
- A `New.zip` file was accidentally committed to `LearningGenAI-Week3` (commit `a54797c`) while adding today's now-superseded progress log — it contains unrelated personal OpenAI Forward-Deployed-Engineer interview prep material (a masterclass deck, an interview guide PDF, 15 system-design case studies).
- That content was unzipped and moved onto its own clean branch, **`OpenAI-FDE-Interview-Prep`** (from `main`), with macOS junk stripped out.
- **`New.zip` itself is still sitting in `LearningGenAI-Week3`'s history**, untouched — you explicitly chose to leave it as-is rather than purge it from history. Worth revisiting before sharing that branch's link with anyone, since it's recoverable from history by anyone with repo access.

## 7. Housekeeping
- **`CLAUDE.md`** at the repo root has been sitting **untracked, uncommitted** all session (an unrelated file from an earlier `/init` task) — never explicitly asked to be committed; still an open decision.
- The old **`PROGRESS_LOG.md`** (a single growing log file) was replaced by this per-date `Daily Recall/` approach and removed.

---

## Open items to pick up next session
1. **Step 3 of the RAG Workflow document is empty** — likely the next thing to build (deep dive + Q&A + interview prep, matching how Step 1 and Step 2 were built).
2. **`Langchain_LCEL.ipynb`** has not been annotated yet, unlike the other three notebooks in that folder.
3. **Decide on `New.zip`** sitting in `LearningGenAI-Week3` history — leave it, or purge it before the repo link is shared further.
4. **Decide on `CLAUDE.md`** — commit it, or leave it local/untracked.
5. If a single fully-consolidated interview file is wanted, the new LangChain/LangGraph section should be added as a Part 3 to `RAG and Vector Database - Interview Questions.md` (and re-uploaded to Drive).
